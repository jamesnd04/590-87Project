# Roboflow Serverless — used by app/api/infer/route.ts (same workflow as inference_sdk.run_workflow)
# Create .env.local from this file. Key must be allowed for serverless inference (see Roboflow dashboard).
# https://app.roboflow.com/settings/api
ROBOFLOW_API_KEY=

# Optional overrides (defaults match project workflow)
# ROBOFLOW_WORKSPACE_NAME=jamess-workspace-vyfdf
# ROBOFLOW_WORKFLOW_ID=small-object-detection-sahi
