# Python deps for scripts/image_infer.py (Ultralytics quad YOLO on dropped images).
# Suggested: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements-ml.txt
ultralytics>=8.3.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
